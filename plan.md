기존에 우리는 사용자가 입력한 정보(영어 단어, 뜻)을 가지고, 시험 공부를 하는데 도와주도록 영어단어와 뜻을 랜덤하게 섞어주는 프로그램을 만들어서 스트림릿으로 배포 했어. 

그런데 그 기능을 고도화 하고 싶어. 
우선, 기존 기능을 수정은 하지 않았으면 좋겠어. 
1. main.py 파일은 주요 함수를 호출하는 역할만하고, 나머지 기능은 분리해서 파일을 별도로 만들어줘. 그래야 나중에 추가, 수정이 되더라도 서로 간섭하지 않으니까.

2. 기본은 main.py 파일이 있고, 기존에 만든 기능은 feature_1 로 옮기고, 새로운 기능은 feature_new 로 만들어서 main.py 에서 호출하도록 해줘.

3. 스트림잇 화면에서는 feature_1 과 feature_new 를 선택할 수 있도록 해줘.

4. feature_new 는 기존 기능에 추가하는 기능이야. 
여기서는 기존과 유사한데, 구글 스프레드시트에서 정보를 가져올거야. 기존에는 사람이 입력을 했지만, new에서는 날짜만 선택하면, 날짜에 해당하는 단어, 뜻을 자동으로 가져와서 섞어 주는 거야. 

5. 구글 스프레드시트는 api 연결을 위한 key는 key 포더에 json 파일로 저장했어. 

